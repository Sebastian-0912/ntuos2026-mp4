# from run_mp2 import public_testcases
# import os

# Register tests
# Note: run_mp2.py functions return the test function, but calling them
# inside @test decorator (which they use) registers them in gradelib.TESTS
# public_testcases(range(25))
